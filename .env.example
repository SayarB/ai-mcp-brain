# Optional Jira (work desk). Copy to `.env` in this repo and fill in.
# Restart MCP after changes. Leave blank to skip Jira — vault today list still works.

JIRA_BASE_URL=https://your-domain.atlassian.net
JIRA_EMAIL=you@example.com
JIRA_API_TOKEN=
